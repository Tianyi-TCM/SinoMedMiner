% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_rules.R
\name{trans_rules}
\alias{trans_rules}
\title{Convert a data frame to a transactions object
Organize wide-format data and convert it to a transactions object.}
\usage{
trans_rules(x)
}
\arguments{
\item{x}{A data frame with the first column as a string ID, the rest being herbs with values of 0 or 1.}
}
\value{
A \code{transactions} object suitable for association rule mining.
}
\description{
This function converts a data frame (with the first column as a string ID column and the rest as herbs with only 0 and 1 values) into a \code{transactions} object for association rule mining.
}
\examples{
df <- data.frame(
  id = c("ID1", "ID2", "ID3"),
  人参 = c(1, 0, 1),
  当归 = c(1, 1, 0),
  川芎 = c(0, 1, 1),
  白芍 = c(1, 0, 1),
  黄芪 = c(0, 1, 0),
  stringsAsFactors = FALSE
)
trans_rules(df)
}
